---
description: Make a component match the reference CSS pixel-perfect
allowed-tools: Read, Write, Edit, Glob, Grep
argument-hint: <component path or class name>
---

Use the `pixel-perfect-ui` subagent to align `$ARGUMENTS` with the rules in
`reference/style.css`, `reference/casino.css`, `reference/theme.css`, and
`reference/responsive.css`. Verify mobile (≤576px), tablet (≤992px), and
desktop (>992px) breakpoints before reporting done.
